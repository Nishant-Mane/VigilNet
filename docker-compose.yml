version: "3.9"

services:
  nids-backend:
    build: ./backend
    container_name: nids_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/storage:/app/storage
    restart: unless-stopped
